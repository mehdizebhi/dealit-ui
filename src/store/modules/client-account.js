
const state = {
    projectSpaces: [],
    profile: {},
};

const getters = {
    projectSpaces(state) {

    },
    projectSpace: (state) => (id) => {},
    jobPositions: (state) => (projectSpaceId) => {},
    jobPosition: (state) => (jobPositionId) => {},
    profile(state) {},
};

const actions = {};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
};